---
name: deploy-skill
description: 运行 Node.js 脚本以部署当前文件夹中的用户项目。仅在 deploy 命令调用时使用。
context: fork
allowed-tools: Bash, Read, Grep
---

# 部署技能

此命令当前不受支持，且不得运行旧版标准部署流程。

### 步骤 1：报告该命令不可用

回复：

```text
❌ `/glm-plan-deploy:deploy` is currently unsupported.
- Use `/glm-plan-deploy:deploy-arbitrary` instead.
```

## 约束

- 不要运行旧版标准部署脚本。
- 不要提供已移除的标准部署说明。
